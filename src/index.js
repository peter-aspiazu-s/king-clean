import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './App';
import './index.scss';

const rootNode = document.getElementById('root');
ReactDOM.render(
  <App />,
  rootNode
);